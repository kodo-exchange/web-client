import { createContext } from 'react'

// DrawerContext is defined in components/layout/index.jsx
// it has 3 properties:
// - isDrawerChecked, setIsDrawerChecked, drawerCheckboxRef
export const DrawerContext = createContext()
